<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COMP5216_Final_Assignment"
        tools:targetApi="31">
        <activity
            android:name=".activities.ProfessionalList"
            android:launchMode="singleTask"
            android:exported="false" />
        <activity
            android:name=".activities.MakeAnAppointment"
            android:launchMode="singleTask"
            android:exported="false" />
        <activity
            android:name=".activities.EditUserProfile"
            android:launchMode="singleTask"
            android:exported="false" />
        <activity
            android:name=".activities.UserProfileActivity"
            android:launchMode="singleTask"
            android:exported="false" />
        <activity
            android:name=".activities.CreatePostActivity"
            android:launchMode="singleTask"
            android:exported="false" />
        <activity
            android:name=".activities.LoginAty"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.PostDetailActivity"
            android:launchMode="singleTask"
            android:label="Post Detail" />
        <activity android:name=".activities.MainActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".activities.RegisterAty"
            android:launchMode="singleTask"/>
        <activity android:name=".activities.EventAty"
            android:launchMode="singleTask"/>
        <activity android:name=".activities.EventDetailsAty"
            android:launchMode="singleTask"/>
        <activity android:name=".activities.EditEventDetailsAty"
            android:launchMode="singleTask"/>
    </application>

</manifest>